# Taruh file ini di public_html/laravel-profile/.htaccess
# Mencegah akses langsung ke .env, vendor, storage, dll. dari browser
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
